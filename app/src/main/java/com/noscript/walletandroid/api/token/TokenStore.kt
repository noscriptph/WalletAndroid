package com.noscript.walletandroid.api.token

/**
 * Clase que representa un almacén de tokens.
 *
 * @property accessToken El token de acceso almacenado.
 */
data class TokenStore(val accessToken: String)